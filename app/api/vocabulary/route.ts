import { NextRequest, NextResponse } from 'next/server';
import { dbConnect } from '@/lib/db';
import { Vocabulary } from '@/models/Vocabulary';
import { auth } from '@/auth';

export async function GET(req:NextRequest){await dbConnect();const sp=req.nextUrl.searchParams;const page=Math.max(1,Math.min(10000,Number(sp.get('page')||1)));const limit=Math.min(100,Math.max(1,Number(sp.get('limit')||30)));const q=(sp.get('q')||'').trim().slice(0,100);const level=sp.get('level');const pos=(sp.get('pos')||'').slice(0,40);const hasMorphology=sp.get('hasMorphology');const filter:any={published:true};if(q){const safe=q.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');filter.$or=[{word:{$regex:safe,$options:'i'}},{lemma:{$regex:safe,$options:'i'}}]}if(level&&/^[1-6]$/.test(level))filter.ceecLevel=Number(level);if(pos)filter.partsOfSpeech=pos;if(hasMorphology==='true')filter['morphology.0']={$exists:true};if(hasMorphology==='false')filter['morphology.0']={$exists:false};const [items,total]=await Promise.all([Vocabulary.find(filter).select('word lemma ipa partsOfSpeech ceecLevel zhTWDefinitions difficulty tags morphology').sort({word:1}).skip((page-1)*limit).limit(limit).lean(),Vocabulary.countDocuments(filter)]);return NextResponse.json({items,page,limit,total,pages:Math.ceil(total/limit)});}
export async function POST(){const session=await auth();if((session?.user as any)?.role!=='admin')return NextResponse.json({error:'Forbidden'},{status:403});return NextResponse.json({error:'Use the validated admin import/editor endpoints'},{status:405});}
