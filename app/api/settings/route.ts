import { NextResponse } from 'next/server';import { auth } from '@/auth';import { dbConnect } from '@/lib/db';import { User } from '@/models/User';import { z } from 'zod';const schema=z.object({theme:z.enum(['light','dark','system']),pronunciation:z.enum(['US','UK']),ttsSpeed:z.number().min(.5).max(2),dailyNewWordGoal:z.number().int().min(1).max(100),dailyReviewGoal:z.number().int().min(1).max(300),soundEffects:z.boolean(),reducedMotion:z.boolean()});export async function PUT(req:Request){const s=await auth();const id=(s?.user as any)?.id;if(!id)return NextResponse.json({error:'Unauthorized'},{status:401});const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Invalid settings',issues:parsed.error.issues},{status:400});await dbConnect();await User.findByIdAndUpdate(id,{$set:{settings:parsed.data}});return NextResponse.json({ok:true});}
