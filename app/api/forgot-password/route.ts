import { NextResponse } from 'next/server';import crypto from 'crypto';import { dbConnect } from '@/lib/db';import { User } from '@/models/User';export async function POST(req:Request){const {email}=await req.json();if(typeof email!=='string')return NextResponse.json({ok:true});await dbConnect();const user=await User.findOne({email:email.toLowerCase()});if(user){const token=crypto.randomBytes(32).toString('hex');user.resetTokenHash=crypto.createHash('sha256').update(token).digest('hex');user.resetTokenExpiresAt=new Date(Date.now()+60*60*1000);await user.save();const base=process.env.APP_URL||process.env.NEXTAUTH_URL;const key=process.env.RESEND_API_KEY;const from=process.env.RESET_EMAIL_FROM;if(base&&key&&from){await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({from,to:[user.email],subject:'Reset your Lexora password',html:`<p>有人要求重設你的 Lexora 密碼。此連結一小時內有效。</p><p><a href="${base}/reset-password?token=${token}">Reset password</a></p><p>若不是你提出要求，請忽略這封信。</p>`})});}}return NextResponse.json({ok:true,message:'If the account exists, a reset email has been sent.'});}
