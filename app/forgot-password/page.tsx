'use client';import { useState } from 'react';export default function ForgotPasswordPage(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMsg('Sending…');const r=await fetch('/api/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const j=await r.json();setMsg(j.message||'If the account exists, a reset email has been sent.')}return <main className="content"><div className="card" style={{maxWidth:520,margin:'40px auto'}}><h1>Forgot password</h1><form onSubmit={submit}><input className="input" type="email" required placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><div className="toolbar"><button className="btn primary">Send reset link</button></div><p className="muted">{msg}</p></form></div></main>}
