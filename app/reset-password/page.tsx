'use client';import { useSearchParams } from 'next/navigation';import { useState } from 'react';export default function ResetPasswordPage(){const token=useSearchParams().get('token')||'';const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();if(password!==confirm){setMsg('Passwords do not match');return}const r=await fetch('/api/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const j=await r.json();setMsg(r.ok?'Password updated. You can now log in.':j.error||'Unable to reset password')}return <main className="content"><div className="card" style={{maxWidth:520,margin:'40px auto'}}><h1>Reset password</h1><form onSubmit={submit}><input className="input" type="password" minLength={8} required placeholder="New password" value={password} onChange={e=>setPassword(e.target.value)}/><input className="input" type="password" minLength={8} required placeholder="Confirm password" value={confirm} onChange={e=>setConfirm(e.target.value)}/><div className="toolbar"><button className="btn primary">Update password</button></div><p className="muted">{msg}</p></form></div></main>}
