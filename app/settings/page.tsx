import { auth } from '@/auth';import { dbConnect } from '@/lib/db';import { User } from '@/models/User';import SettingsForm from '@/components/SettingsForm';export default async function SettingsPage(){const s=await auth();await dbConnect();const u=await User.findById((s?.user as any)?.id).lean();return <main className="content"><h1>Settings</h1><SettingsForm initial={u?.settings||{theme:'system',pronunciation:'US',ttsSpeed:1,dailyNewWordGoal:10,dailyReviewGoal:30,soundEffects:true,reducedMotion:false}}/></main>}
