'use client';
import { useState } from 'react';
export default function SettingsForm({initial}:{initial:any}){const [v,setV]=useState(initial);const [status,setStatus]=useState('');async function save(e:React.FormEvent){e.preventDefault();setStatus('Saving…');const r=await fetch('/api/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(v)});setStatus(r.ok?'Saved':'Unable to save')}return <form className="card" onSubmit={save}><label>Theme<select className="select" value={v.theme} onChange={e=>setV({...v,theme:e.target.value})}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label><label>Pronunciation<select className="select" value={v.pronunciation} onChange={e=>setV({...v,pronunciation:e.target.value})}><option>US</option><option>UK</option></select></label><label>TTS speed<input className="input" type="number" min="0.5" max="2" step="0.1" value={v.ttsSpeed} onChange={e=>setV({...v,ttsSpeed:Number(e.target.value)})}/></label><label>Daily new words<input className="input" type="number" min="1" max="100" value={v.dailyNewWordGoal} onChange={e=>setV({...v,dailyNewWordGoal:Number(e.target.value)})}/></label><label>Daily reviews<input className="input" type="number" min="1" max="300" value={v.dailyReviewGoal} onChange={e=>setV({...v,dailyReviewGoal:Number(e.target.value)})}/></label><label><input type="checkbox" checked={v.soundEffects} onChange={e=>setV({...v,soundEffects:e.target.checked})}/> Sound effects</label><label><input type="checkbox" checked={v.reducedMotion} onChange={e=>setV({...v,reducedMotion:e.target.checked})}/> Reduced motion</label><div className="toolbar"><button className="btn primary">Save settings</button><span className="muted">{status}</span></div></form>}
